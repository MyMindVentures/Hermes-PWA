"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Activity, Bot, CheckCircle2, CircleDot, GitBranch, GitPullRequest, History, Plus, Rocket, Search, Sparkles, Terminal } from "lucide-react";
import { demoProjects, type Project } from "@providers/data-provider";
import { motion } from "framer-motion";

export function ProjectDashboard() {
  const [query, setQuery] = useState("");
  const filtered = demoProjects.filter((p) => `${p.name} ${p.description}`.toLowerCase().includes(query.toLowerCase()));
  return <main className="min-h-screen bg-[#090d18] px-6 py-10 text-slate-100 md:px-10"><header className="mx-auto flex max-w-7xl items-end justify-between gap-6"><div><p className="mb-3 font-mono text-[10px] tracking-[.18em] text-emerald-400"><span className="mr-2">●</span> ALL SYSTEMS OPERATIONAL</p><h1 className="text-3xl font-bold tracking-tight">Good morning, Parallax <span className="text-violet-400">✦</span></h1><p className="mt-2 text-sm text-slate-400">Your AI project command center.</p></div><button className="hidden items-center gap-2 rounded-lg bg-violet-500 px-4 py-2 text-sm font-bold shadow-lg shadow-violet-500/20 md:flex"><Plus size={16} /> New project</button></header><section className="mx-auto mt-10 grid max-w-7xl grid-cols-2 gap-3 lg:grid-cols-4">{[["Total projects", "04", Rocket, "text-violet-400"], ["Open issues", "47", CircleDot, "text-amber-400"], ["Active deployments", "03", Activity, "text-emerald-400"], ["AI conversations", "128", Bot, "text-blue-400"]].map(([label, value, Icon, color]) => <div className="rounded-xl border border-slate-800 bg-slate-900/70 p-4" key={String(label)}><div className={`mb-3 w-fit rounded-lg bg-white/5 p-2 ${color as string}`}><Icon size={17} /></div><p className="text-xs text-slate-400">{label as string}</p><strong className="block text-2xl">{value as string}</strong><small className="text-[10px] text-emerald-400">Healthy</small></div>)}</section><div className="mx-auto mt-12 flex max-w-7xl items-center justify-between"><div><h2 className="text-lg font-bold">Your projects</h2><p className="mt-1 text-xs text-slate-400">Manage and monitor your active workspaces.</p></div><label className="hidden items-center gap-2 rounded-lg border border-slate-800 bg-slate-900 px-3 py-2 text-xs text-slate-400 sm:flex"><Search size={15} /><input className="w-36 bg-transparent outline-none" placeholder="Search projects" value={query} onChange={(e) => setQuery(e.target.value)} /></label></div><div className="mx-auto mt-5 grid max-w-7xl gap-4 sm:grid-cols-2 xl:grid-cols-4">{filtered.map((project) => <ProjectCard key={project.id} project={project} />)}<button className="flex min-h-[285px] flex-col items-center justify-center rounded-xl border border-dashed border-slate-700 text-slate-400"><span className="mb-3 rounded-full border border-dashed border-slate-600 p-3"><Plus size={20} /></span><strong className="text-xs text-slate-200">Add a new project</strong><span className="mt-2 text-[10px]">Connect a GitHub repository</span></button></div><section className="mx-auto mt-12 max-w-7xl"><h2 className="text-lg font-bold">Recent activity</h2><div className="mt-4 overflow-hidden rounded-xl border border-slate-800"><ActivityRow icon={<GitPullRequest size={15} />} title="Pull request merged" detail="feat: add project health monitoring" project="CostaPulse" color="text-emerald-400" /><ActivityRow icon={<CircleDot size={15} />} title="Issue opened" detail="Improve mobile navigation layout" project="Hermes PWA" color="text-amber-400" /><ActivityRow icon={<Rocket size={15} />} title="Deployment successful" detail="Production build deployed" project="Nexus Mobile" color="text-blue-400" /></div></section></main>;
}
function ProjectCard({ project }: { project: Project }) { const router = useRouter(); return <motion.button whileHover={{ y: -3 }} onClick={() => router.push(`/projects/show/${project.id}`)} className="rounded-xl border border-slate-800 bg-gradient-to-br from-slate-900 to-[#0d1422] p-5 text-left transition hover:border-slate-600"><div className="flex items-center"><div className="rounded-lg bg-violet-500/10 p-2.5 text-violet-400"><Terminal size={19} /></div><span className={`ml-3 text-[10px] font-mono uppercase ${project.status === "online" ? "text-emerald-400" : project.status === "development" ? "text-amber-400" : "text-slate-500"}`}>● {project.status}</span></div><h3 className="mt-5 text-sm font-bold">{project.name}</h3><p className="mt-3 min-h-10 text-xs leading-5 text-slate-400">{project.description}</p><div className="mt-4 flex flex-wrap gap-1.5">{project.technologies.map((technology) => <span className="rounded bg-slate-800 px-2 py-1 font-mono text-[9px] text-slate-400" key={technology}>{technology}</span>)}</div><div className="mt-5 flex justify-between text-[10px] text-slate-500"><span>Progress</span><strong className="font-mono text-slate-200">{project.progress}%</strong></div><div className="mt-2 h-1 rounded bg-slate-800"><span className="block h-full rounded bg-violet-400" style={{ width: `${project.progress}%` }} /></div><div className="mt-5 flex justify-between border-t border-slate-800 pt-3 font-mono text-[9px] text-slate-500"><span><CircleDot className="mr-1 inline" size={12} />{project.openIssues} issues</span><span><Sparkles className="mr-1 inline text-violet-400" size={12} />{project.hermesStatus}</span></div></motion.button> }
function ActivityRow({ icon, title, detail, project, color }: { icon: React.ReactNode; title: string; detail: string; project: string; color: string }) { return <div className="flex items-center gap-3 border-b border-slate-800 bg-slate-900/50 p-4 last:border-0"><div className={`rounded-lg bg-white/5 p-2 ${color}`}>{icon}</div><div><strong className="block text-xs">{title}</strong><span className="text-[10px] text-slate-400">{detail}</span></div><span className="ml-auto rounded bg-slate-800 px-2 py-1 font-mono text-[9px] text-slate-400">{project}</span></div> }
